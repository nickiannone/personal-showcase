import { render } from "@testing-library/react";
import DateRange from './DateRange';

it('should render without crashing', () => {
    render(<DateRange />);
});

it('should be able to load with no values', () => {

});

it('should be able to load with valid values', () => {

});

it('should reset to empty if loaded with invalid values', () => {

});

it('should reset to empty if loaded with out-of-order values', () => {

});
