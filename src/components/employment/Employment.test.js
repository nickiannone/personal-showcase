// TODO Separate unit from integration tests!
describe("Employment unit tests", () => {
    it("should show the display name, start date, end date, etc.", async () => {

    });

    it("should show the edit button and delete button, if editable", async () => {

    });

    it("should edit and save with valid inputs, if editable", async () => {

    });

    it("should fail to edit with invalid inputs, if editable", async () => {

    });
    
    // TODO Break out the EmploymentEditor into a different component!

    it("should delete and confirm, if editable", async () => {

    });

    it("should delete and cancel, if editable", async () => {

    });

    // TODO Break out the DeleteConfirmDialog into a different component!

    it("should handle delete request failure", async () => {

    });

    it("should handle edit request failure", async () => {
        
    });
});